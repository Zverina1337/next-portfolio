---
name: gsap-review
description: Проверка GSAP анимаций на конфликты, производительность и accessibility. Использовать перед коммитом анимаций, при багах с "прыгающими" элементами, или для оптимизации производительности.
allowed-tools: Read, Glob, Grep
---

# GSAP Review

Автоматическая проверка GSAP анимаций на основе архитектурных паттернов проекта и распространенных проблем.

## Что проверяется

### 1. Конфликты анимаций
- **Проблема**: Одновременные анимации на одном элементе вызывают "прыжки"
- **Решение**: Использование `gsap.killTweensOf()` перед новыми анимациями
- **Паттерн**: Проверка на наличие `killTweensOf` в функциях toggle/animate

### 2. ScrollTrigger пороги
- **Проблема**: Низкие пороги (`start: 'top 80%'`) не срабатывают для последних элементов
- **Решение**: Оптимальные значения `start: 'top 90%'` или выше
- **Паттерн**: Анализ всех ScrollTrigger конфигураций

### 3. Duration и Easing
- **Проблема**: Резкие анимации (< 0.5s), агрессивные easing (elastic, power4)
- **Решение**: Duration 0.7-0.9s, мягкие easing (`power1.inOut`, `power2.out`)
- **Паттерн**: Проверка конфигураций timeline и tween

### 4. Height анимации
- **Проблема**: `height: 'auto'` не работает с GSAP
- **Решение**: Использование `maxHeight` с фиксированным значением
- **Паттерн**: Поиск анимаций height без maxHeight альтернативы

### 5. Accessibility
- **Проблема**: Анимации игнорируют `prefers-reduced-motion`
- **Решение**: Проверка медиа-запроса и отключение/упрощение анимаций
- **Паттерн**: Наличие условий для reduced motion

### 6. Performance
- **Проблема**: Отсутствие оптимизации рендеринга
- **Решение**: GPU acceleration (`will-change`, `transform`, `opacity`)
- **Паттерн**: Проверка использования transform вместо top/left

### 7. Cleanup
- **Проблема**: Memory leaks из-за не очищенных анимаций
- **Решение**: Использование `gsap.context()` с cleanup в useEffect
- **Паттерн**: Проверка наличия return cleanup функций

## Инструкции по проверке

### Шаг 1: Найти все файлы с GSAP
```bash
# Файлы с gsap импортами и использованием
```
Использовать `Grep` с паттерном `gsap\.(to|from|timeline|context)` и `Glob` для `**/*.tsx`, `**/*.ts`.

### Шаг 2: Анализ каждого файла
Для каждого файла проверить:

1. **Конфликты**:
   - Есть ли функции с множественными анимациями одного элемента?
   - Используется ли `killTweensOf` перед анимациями?
   - Особое внимание на toggle/expand/collapse функции

2. **ScrollTrigger**:
   - Найти все `ScrollTrigger.create` или `scrollTrigger: {}`
   - Проверить значения `start` (должно быть >= 85-90%)
   - Проверить `toggleActions` (рекомендуется `'play none none reverse'`)

3. **Duration/Easing**:
   - Найти все `duration:` значения
   - Флаг если < 0.5 (слишком быстро) или > 1.5 (слишком медленно)
   - Проверить easing: избегать `elastic`, `bounce` для UI анимаций

4. **Height**:
   - Поиск паттерна `height: 'auto'` или `height: "auto"`
   - Предложить замену на `maxHeight`

5. **Accessibility**:
   - Поиск `prefers-reduced-motion` или `matchMedia`
   - Если анимаций много, а проверки нет — флаг

6. **Cleanup**:
   - В React компонентах: наличие `gsap.context()` и `return () => ctx.revert()`
   - В useEffect: наличие cleanup функции

### Шаг 3: Проверка Master Timeline интеграции
Согласно архитектуре проекта (`components/hooks/useMasterTl.ts`):
- Проверить, интегрируются ли новые анимации с master timeline через `getMasterTl()`
- Особенно для компонентов, которые анимируются при загрузке страницы

### Шаг 4: Генерация отчета

Формат вывода:

```
## GSAP Review Results

### ✓ Passed Checks
- [Filename]: Корректное использование killTweensOf
- [Filename]: Оптимальные ScrollTrigger пороги

### ⚠ Warnings
- [Filename:line]: Duration слишком короткая (0.3s), рекомендуется 0.7-0.9s
- [Filename:line]: Агрессивный easing 'elastic.out', рекомендуется 'power1.inOut'

### ✗ Critical Issues
- [Filename:line]: Отсутствует killTweensOf в toggle функции
- [Filename:line]: ScrollTrigger start: 'top 80%' — слишком низкий порог
- [Filename:line]: Анимация height: 'auto' — использовать maxHeight
- [Filename:line]: Нет проверки prefers-reduced-motion

### 📊 Summary
- Files checked: X
- Total animations: Y
- Issues found: Z
- Performance score: A/B (например, 8/10)
```

## Примеры проблемного кода

### ❌ Плохо: Конфликт анимаций
```typescript
const toggleExpand = () => {
  // Нет killTweensOf — анимации конфликтуют!
  gsap.to(card, { scaleX: 1.05, duration: 0.9 })
  gsap.to(card, { scaleY: 1.08, duration: 0.9 })
}
```

### ✅ Хорошо: С предотвращением конфликтов
```typescript
const toggleExpand = () => {
  gsap.killTweensOf([card, glow, expanded]) // Очистка перед анимацией

  const tl = gsap.timeline({ defaults: { ease: 'power1.out' } })
  tl.to(card, { scaleX: 1.05, scaleY: 1.08, duration: 0.9 }, 0)
}
```

### ❌ Плохо: Низкий ScrollTrigger порог
```typescript
ScrollTrigger.create({
  trigger: element,
  start: 'top 80%', // Последние элементы могут не сработать
})
```

### ✅ Хорошо: Оптимальный порог
```typescript
ScrollTrigger.create({
  trigger: element,
  start: 'top 90%', // Срабатывает раньше
  toggleActions: 'play none none reverse',
})
```

### ❌ Плохо: Height анимация
```typescript
gsap.to(element, { height: 'auto' }) // Не работает!
```

### ✅ Хорошо: MaxHeight анимация
```typescript
gsap.to(element, { maxHeight: '1000px', duration: 0.7 })
```

### ❌ Плохо: Нет cleanup
```typescript
useEffect(() => {
  gsap.to(ref.current, { opacity: 1 })
  // Memory leak!
}, [])
```

### ✅ Хорошо: С cleanup
```typescript
useEffect(() => {
  const ctx = gsap.context(() => {
    gsap.to(ref.current, { opacity: 1 })
  })

  return () => ctx.revert() // Очистка при unmount
}, [])
```

## Референсы из проекта

- **Master Timeline паттерн**: `components/hooks/useMasterTl.ts`
- **Примеры правильных анимаций**: `app/about/components/TimelineCard.tsx`
- **ScrollTrigger оптимизация**: `app/about/components/AboutTimeline.tsx`
- **Accessibility**: `app/(home)/components/BlockIntro.tsx` (проверка reduced motion)

## Когда использовать этот навык

1. **Перед коммитом**: Проверка новых анимаций на соответствие стандартам
2. **При багах**: Диагностика "прыгающих" элементов, не срабатывающих анимаций
3. **Code review**: Проверка pull request с анимациями
4. **Оптимизация**: Поиск узких мест в производительности
5. **Onboarding**: Обучение новых разработчиков паттернам проекта

## Метрики качества

После проверки оцениваю анимации по шкале:

- **10/10**: Идеально — все чеки пройдены
- **8-9/10**: Отлично — минорные warnings
- **6-7/10**: Хорошо — есть warnings, требуется оптимизация
- **4-5/10**: Удовлетворительно — критичные проблемы требуют исправления
- **< 4/10**: Требуется рефакторинг

## Ограничения

- Не могу запустить тесты в браузере (полагаюсь на ваш фидбек)
- Не имею доступа к production метрикам
- Проверяю только статический код, не runtime поведение
