/**
 * Shared типы для Skills пирамиды
 * Используется в 3D (Three.js) и 2D (Canvas) версиях
 */

export type Skill = {
  /** Название навыка (отображается на кубе) */
  name: string
  /** Путь к иконке навыка (для Canvas версии) */
  icon: string
}
